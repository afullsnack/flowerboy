// @ts-check
import { defineConfig } from 'astro/config';

// Static output (default). The site is deployed to Vercel, which detects the
// Astro framework and serves the prebuilt `dist/` directory — see vercel.json.
export default defineConfig({});
